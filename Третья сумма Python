import math

def main():
    n_max = 10
    total_sum = 0.0
    for i in range(1, n_max + 1):
        total_sum += (1.0 / (36 * i * i - 24 * i - 5))
    print("Summa:", total_sum)

if __name__ == "__main__":
    main()
